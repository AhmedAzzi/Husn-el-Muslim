<?xml version="1.0" encoding="utf-8"?>
<!-- Translucent gold pill for the prayer-alert badge. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/overlay_tafsir_fill" />
    <corners android:radius="20dp" />
    <stroke
        android:width="1dp"
        android:color="@color/overlay_gold_faint" />
</shape>
