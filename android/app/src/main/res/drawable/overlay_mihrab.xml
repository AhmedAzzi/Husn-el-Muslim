<?xml version="1.0" encoding="utf-8"?>
<!-- Mihrab arch: dark app surface with a gold stroke, a tall rounded crown
    echoing a mosque arch, soft square base. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/overlay_surface" />
    <corners
        android:topLeftRadius="120dp"
        android:topRightRadius="120dp"
        android:bottomLeftRadius="24dp"
        android:bottomRightRadius="24dp" />
    <stroke
        android:width="2dp"
        android:color="@color/overlay_gold" />
</shape>
